/**
 * @name ConfigProgressPlugin
 * @description 构建显示进度条
 */

import { PROGRESS } from '../../constant';

export const ConfigProgressPlugin = () => {
  // vite-plugin-progress 在新版本中可能不兼容，暂时禁用
  // 可以使用 Vite 内置的进度显示
  return [];
};
